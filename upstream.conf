# Voiceboard upstream configuration

# ===== Upstream (AIM HOST) =====
UPSTREAM_SCHEME=http
UPSTREAM_HOST=172.27.126.244
UPSTREAM_PORT=8000

# ===== Local UI/Proxy server =====
SERVER_HOST=0.0.0.0
SERVER_PORT=8888

# ===== Timeouts (seconds) =====
# General proxy timeout (info/balance/list-voices/etc.)
RELAY_TIMEOUT=60
# Longer timeout for /speak (TTS can take longer)
SPEAK_TIMEOUT=600
